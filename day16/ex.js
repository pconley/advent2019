var _ = require('lodash');

// console.log(_.range(-12, 12).map(x=>`${"0"+x}`))

// const index = 3;

// const itemCount = _(datasets).map(ds => ds.data[index].raw).compact().value().length;

const value = '<1';

console.log(value, +value, +value===100);